# --- Cream Dream Frontend Environment Variables ---

# Base URL for the API (if different from the frontend origin)
# Example: http://localhost:5000
VITE_API_URL=

# Note: In development, Vite uses the proxy setting in vite.config.js
# In production, these variables must be set in your hosting provider (Vercel, Netlify, etc.)
